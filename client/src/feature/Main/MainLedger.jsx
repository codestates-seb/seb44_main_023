import { styled } from "styled-components";

const MainLedger = () => {
  return <StyledWrapper>가계부</StyledWrapper>;
};

export default MainLedger;

const StyledWrapper = styled.div`
  width: 50%;
`;
